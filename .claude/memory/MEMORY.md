# Memory Index

One line per memory; the fact itself lives in the linked file. Newest additions at the
bottom. Read this at session start — `.claude/CLAUDE.md` points here.

- [Memory lives in the repo](memory-lives-in-the-repo.md) — write memories to `.claude/memory/`, not the per-device dir, so they survive a device switch
- [Coauthor trailer only on large commits](commit-coauthor-only-on-large-commits.md) — small commits the user could have written get no `Co-Authored-By: Claude`
